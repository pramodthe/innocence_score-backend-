services:
  - type: web
    name: innocence-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main_bert:app --host 0.0.0.0 --port $PORT"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"